// This params will override the values defined in the main workflow script.
params.foo = 'Bonjour'
params.bar = 'le monde!'

// The env values will be exported into the environment where the workflow tasks will be executed.
env.ALPHA = 'greek life'
env.BETA = "$HOME/some/path"